@{
    ViewBag.Title = @AccountStrings.Get("RestoreTitle");
    
} 
<div class="container">    
	<div class="row bk-main"> 	      
        <div class="col-lg-12 col-md-12" style="height: 80px;">
			<h1>@ViewBag.Title</h1>				
            <hr />
		</div>
        <div class="col-lg-12 col-md-12">                     
            <h3 style="color:#8c8c8c; margin-top:0;">@AccountStrings.Get("Register_ChangePassword")</h3>
        </div>
        <div class="clearfix" style="margin-bottom:10px;"></div>	
        <div class="col-lg-12 col-md-12">         
            @if (!string.IsNullOrEmpty(ViewBag.ReturnUrl))
            {            
                 <button name="submit" id="submit" value="" type="submit" class="btn btn-large btn-primary" onclick="window.location='@ViewBag.ReturnUrl';">@AccountStrings.Get("Restore_MaimFormLink")</button>        
            }
            else
            {
                <button name="submit" id="submit" value="" type="submit" class="btn btn-large btn-primary" onclick="window.location='@Url.Action("index", "welcome")';">@AccountStrings.Get("Restore_MaimFormLink")</button>                    
            }
        </div>
    </div>
</div>